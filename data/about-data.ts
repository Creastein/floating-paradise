import { LucideIcon } from 'lucide-react'

// ── Types ──────────────────────────────────────────────────────────

export interface GreenPractice {
  icon: LucideIcon
  title: string
  desc: string
}

export interface CoreValue {
  icon: LucideIcon
  title: string
  desc: string
}
